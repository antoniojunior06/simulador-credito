import { MoedaDinamicaDirective } from './moeda-dinamica.directive';

describe('MoedaDinamicaDirective', () => {
  it('should create an instance', () => {
    const directive = new MoedaDinamicaDirective();
    expect(directive).toBeTruthy();
  });
});
